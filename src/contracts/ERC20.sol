pragma solidity >=0.4.21 <0.7.0;

contract ERC20 {

    //function transfer(address _to, address _from) public returns (bool Succeed);
    event Transfer(address indexed _from, address indexed _to, uint256 _value);

    //function transferFrom(address _to, address _from, uint256 _amount) public returns (bool Succeed);
}